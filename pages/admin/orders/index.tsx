import React from "react";
import { AdminLayout } from "../../../components/common";

interface Props {}

const OrderList = (props: Props) => {
  return <div>Product List</div>;
};

OrderList.Layout = AdminLayout;

export default OrderList;
